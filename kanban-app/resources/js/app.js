// Importa Bootstrap (via NPM)
import 'bootstrap';

// Importa jQuery (via NPM)
import $ from 'jquery';

window.$ = $;
window.jQuery = $;

// Importa lógicas específicas
import './login';
import './boards/index';
import './boards/show';